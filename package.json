{
  "name": "kscan-glasses-webapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copy:wasm": "node scripts/copy-mediapipe-wasm.js",
    "verify:models": "node scripts/verify-models.js",
    "download:models": "node scripts/download-models.js",
    "postinstall": "node scripts/copy-mediapipe-wasm.js",
    "test:static": "node scripts/static-tests.js",
    "test:contract": "node scripts/contract-tests.js",
    "test": "npm run verify:models && npm run build && npm run test:static && npm run test:contract"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
